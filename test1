javascript: (function() {    var box = document.createElement("div");    box.style.width = "200px";    box.style.height = "300px";    box.style.backgroundImage = "url('https://thumbs.gfycat.com/AgonizingObviousHochstettersfrog-max-1mb.gif')";    box.style.backgroundSize = "cover";    box.style.position = "absolute";    box.style.top = "0";    box.style.left = "0";    box.style.resize = "both";    box.style.overflow = "auto";    box.title = "Welcome";    document.body.appendChild(box);    var innerBox = document.createElement("div");    innerBox.style.width = "50px";    innerBox.style.height = "50px";    innerBox.style.backgroundColor = "black";    innerBox.style.color = "white";    innerBox.style.textAlign = "center";    innerBox.innerHTML = "Run Code";    box.appendChild(innerBox);    var java1 = "javascript:(function() { var box2 = document.createElement('div'); box2.style.width = '200px'; box2.style.height = '300px'; box2.style.background = 'green'; box2.style.backgroundSize = 'cover'; box2.style.position = 'absolute'; box2.style.top = '0'; box2.style.left = '0'; box2.style.resize = 'both'; box2.style.overflow = 'auto'; box2.title = 'Welcome'; document.body.appendChild(box2); var offset = {}; var shiftClick = false; var locked = false; box2.addEventListener('mousedown', function(event) { if(!locked) { offset.x = event.clientX - box2.offsetLeft; offset.y = event.clientY - box2.offsetTop; document.addEventListener('mousemove', moveBox2); } }); document.addEventListener('mouseup', function() { document.removeEventListener('mousemove', moveBox2); }); function moveBox2(event) { box2.style.left = event.clientX - offset.x + 'px'; box2.style.top = event.clientY - offset.y + 'px'; } document.addEventListener('keydown', function(event) { if (event.keyCode === 16) { if(shiftClick) { shiftClick = false; box2.style.display = 'block'; } else { shiftClick = true; box2.style.display = 'none'; } } if (event.keyCode === 17) { locked = !locked; } });})();";    innerBox.addEventListener('click', function() {        eval(java1);    });    var offset = {};    var shiftClick = false;    var locked = false;    box.addEventListener("mousedown", function(event) {        if (!locked) {            offset.x = event.clientX - box.offsetLeft;            offset.y = event.clientY - box.offsetTop;            document.addEventListener("mousemove", moveBox);        }    });    document.addEventListener("mouseup", function() {        document.removeEventListener("mousemove", moveBox);    });    function moveBox(event) {        box.style.left = event.clientX - offset.x + "px";        box.style.top = event.clientY - offset.y + "px";    }    document.addEventListener("keydown", function(event) {        if (event.keyCode === 16) {            if (shiftClick) {                shiftClick = false;                box.style.display = "block";            } else {                shiftClick = true;                box.style.display = "none";            }        }        if (event.keyCode === 17) {            locked = !locked;        }    });  var button = document.createElement("button");button.innerHTML = "Create iframe";box.appendChild(button);button.addEventListener("click", function() {  var iframe = document.createElement("iframe");  iframe.style.width = "200px";  iframe.style.height = "150px";  iframe.style.position = "absolute";  iframe.style.top = "50px";  iframe.style.left = "50px";  iframe.style.resize = "both";  iframe.style.overflow = "auto";  iframe.src = ("https://3kh0.github.io/projects/alienhominid/index.html");  document.body.appendChild(iframe);  var iframeOffset = {};iframe.addEventListener("mousedown", function(event) {  if (!iframeLocked) {    iframeOffset.x = event.clientX - iframe.offsetLeft;    iframeOffset.y = event.clientY - iframe.offsetTop;    document.addEventListener("mousemove", moveIframe);  }});document.addEventListener("mouseup", function() {  document.removeEventListener("mousemove", moveIframe);});function moveIframe(event) {  iframe.style.left = event.clientX - iframeOffset.x + "px";  iframe.style.top = event.clientY - iframeOffset.y + "px";}  document.addEventListener("keydown", function(event) {  if (event.keyCode === 16) {    if (shiftClick) {      shiftClick = false;      iframe.style.display = "block";    } else {      shiftClick = true;      iframe.style.display = "none";    }  }  if (event.keyCode === 17) {    iframeLocked = !iframeLocked;  }});document.addEventListener("keydown", function(event) {  if (event.keyCode === 16) {    if (shiftClick) {      shiftClick = false;      iframe.style.display = "block";    } else {      shiftClick = true;      iframe.style.display = "none";    }  }  if (event.keyCode === 17) {    iframeLocked = !iframeLocked;  }});});})();
